/**
 * Interview setup — pick type, job role, start session.
 * Full implementation in ST-17.
 */
export default function InterviewSetupPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-bold text-gray-900">Setup Interview</h1>
      <p className="mt-2 text-sm text-gray-500">Full implementation in ST-17</p>
    </main>
  );
}
